# Your application name
spring.application.name=instaClone

# ===================================================================
# MySQL DATABASE CONFIGURATION
# ===================================================================

# Database Connection URL (change 'insta_clone_db' if you named it differently)
spring.datasource.url=jdbc:mysql://localhost:3306/insta_clone_db

# Database Credentials (use the root username and the password you set)
spring.datasource.username=root
spring.datasource.password=Bhaumik0777

# ===================================================================
# JPA/HIBERNATE CONFIGURATION
# ===================================================================

# This tells Hibernate to update the database schema to match your entities
spring.jpa.hibernate.ddl-auto=update

# (Optional) Shows the SQL queries in the console for debugging
spring.jpa.show-sql=true